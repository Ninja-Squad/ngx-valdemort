dist: trusty
language: node_js
node_js:
  - lts/*
addons:
  chrome: stable
script:
  - yarn build
  - yarn test
  - yarn demobuild
  - yarn demotest
  - yarn doc
after_success:
# upload js coverage to codecov.io
- yarn codecov

